# CLAUDE.md

這份文件提供 Claude Code（claude.ai/code）在這個儲存庫中工作時的指引。

### 核心語氣

我希望你像是 MSN 時代聊天室裡面對話那樣的輕鬆風格，並且盡可能使用貼近於我的寫作方式來跟我說話。

## 概述

這是一個實作 Zettelkasten（卡片筆記盒）個人知識管理系統的 Obsidian 資料庫。

## 資料夾結構

- **000 SYSTEM/** — 系統定義文件（系統藍圖、專案工作系統架構圖）
- **001 INBOX/** — 所有新檔案的預設位置。筆記在處理與搬移前的暫存區。
- **002 FREEWRITE/** — 自由寫作／日期類筆記（不納入 git）
- **003 ATLAS/** — 索引檔（例如 `Index 1 - 寫作的技術`、`Index 11.2 - 提詞設計`）。這些是用來組織與連結相關筆記的 Maps of Content（MOC）。**Reading 待讀清單文件也放這裡**——當要建立某個索引頁的待讀清單（例如把「正在閱讀／待讀」的書單獨立成一份文件）時，一律存到 `003 ATLAS/`，並在對應索引頁建立連結。
- **004 PROJECT/** — 主要專案檔案（DASHBOARD）
- **005 CARDS/** — 主要的 Zettelkasten 卡片。系統的核心永久筆記。
- **006 PROJECT CARDS/** — 專案相關卡片（平鋪）。
- **007 EXTRAS/** — 額外資源與附件。子資料夾包含 `993 Claude Skills`、`994 Archived`、`995 Prompts`、`996 Files`、`998 Reference`、`999 Template`
- **008 SOURCE/** — 原始素材與書籍筆記。書可以是單一檔案（例如 `《Eric Jorgenson - The Almanack of Naval Ravikant⋯⋯》.md`），也可以是以書名命名的資料夾（例如 `《The Simple Path to Wealth》/`）
- **009 草稿區/** — 粗稿
- **010 文章區/** — 已發布的文章
- **011 思考箱/** — 思考／反思筆記，也是靈感筆記的最終歸宿
- **012 詩/** — 詩作草稿


## 卡片筆記操作原則（AI 協作脈絡）


### 一、新卡片寫入：簡短、草率、核心想法

**原則**：一張卡片的內容應是好讀易懂，簡短明白。

**邏輯**：
- 魯曼用 A6 卡片，物理上無法寫長；他的卡片大多只有兩三句，很多只有一句話。
- 一張卡片如果寫成一整篇文章，脈絡就固型了，很難被多重儲存（見原則三）
- 在數位工具上可以不限長度，但長度越長，理解成本就越大，必須注意這點。
- 反過來說：如果一張卡片可以被多重儲存，代表它本來兩三句能講完；你用 1000 字寫反而讓它死掉

結論：「卡片怎樣寫才算好？」——輕鬆寫、突發靈感也行，不需要完整、不需要寫到可以發脆、不需要有任何工整。**澄清的工作交給整個筆記盒，不交給單張卡片。**

**AI 操作指引**：
- 不要替使用者潤飾、工整化、或擴寫成「完整版」，盡可能保留使用者自己的語氣與用詞。
- 不要把 1000 字的東西當一張卡片放進去——如果真的有那麼多話，那是文章稿，不是卡片

---

### 二、局部連結，不做分類

**原則**：將新卡片放入筆記盒時，不問「這張該歸哪個索引頁」，只問「這張可以連到哪些既有卡片」。

**邏輯**：
- 魯曼寫新卡片時不會想「這屬於哪個主題區塊」；他只想「這張可以接在哪張舊卡後面」，然後沿用舊卡編號
- 結果：一個編號區塊裡的主題會自然分叉（魯曼稱為「分叉能力」Verzweigungsfähigkeit，Schmidt 譯為 forking capacity）——例如他的第 21 區塊原本是「功能主義」，卻會一路走到「階級社會」
- **編號只是地址，地址不會意味著這裡的住戶是什麼個性**——卡片的意義由連結脈絡決定，不由位置決定
- 好處是速度：分類決策越簡單 → 寫入摩擦越小 → 願意寫下的想法越多 → 系統越豐富

結論：「這張筆記應該歸類到哪一個索引頁下面？」——現在知道不需要歸類到索引頁下面，只需要找到既有筆記給它連結。

**AI 操作指引**：
- 對新卡片要做的判斷是：它可以跟哪些既有卡片產生有意義的連結？

---

### 三、多重儲存

**原則**：想到三種連結可能，就在三種位置都建。不要找「最正確」的那個位置。

**邏輯**：
1. **降低遺失風險**：同一個主題可以從多個入口找到
2. **脈絡構成意義**：同一張卡片放在「決策理論」區塊 vs「古代社會」區塊，會被周圍的卡片賦予不同的解讀意義
3. **未來開放性**：不把自己鎖死在剛性分類裡，系統可以自然演化

魯曼原文 (9/8b2)：「多重儲存是存放複雜資訊的必要手段。」

**AI 操作指引**：
- 幫加恩找連結時，給出所有合理的候選，不要只給「最貼切」的那張——讓他選擇哪幾個值得建。
- 複雜主題優先多點建連結；簡單、獨特的主題一個連結也可以

---

## 四、不刪除筆記：卡片盒是化糞池

**原則**：遇到你認為「錯」的舊筆記，不要刪除；在後面新增一張卡片說明為什麼錯。

**邏輯**：
- 魯曼原文 (9/8a2)：「卡片筆記是化糞池——不要只放那些精心處理好的卡片。暫停檢驗與決策，這也是速度的問題。」
- 「錯誤」是當下的脈絡判定的——脈絡變了，錯誤可能翻案
- 卡片的內容本身不是重點，它被放在什麼脈絡下解讀才是重點
- 筆記盒不是「精品店」（只放處理好的），是「化糞池」（澄清的工作由整體系統完成，不由單張卡片完成）

**AI 操作指引**：
- 絕對不要刪除加恩已有的卡片，即使它看起來過時、錯誤、或重複
- 遇到「錯」的舊卡 → 新增反駁卡，連結到原卡
- 遇到重複內容則為例外 → 應選擇較少連結的那張刪除。

---

## 五、索引頁是入口，不是目錄

**原則**：索引頁由關鍵字組成，一個關鍵字指向 1~4 張入口卡（大多 1~2 張，只有很大的主題才到 4 張）。

**邏輯**：
- 魯曼 9 萬張卡片只對應約 3200 個關鍵字索引——索引是刻意保持不完整的
- 索引不是為了列出所有相關卡片，而是提供進入卡片網絡的「入口點」
- 通過一個關鍵字找到 1~4 張入口卡，然後順著卡片上的連結遊走，遲早會走到所有相關內容
- 入口卡可以是 hub 卡、深層子節點、甚至人名卡——任何能快速帶人進入網絡的卡都行

關鍵字指向邏輯：
- 當關鍵字慨念可以跨多個主題 → 指向多個大區的卡片筆記
- 當關鍵字在單一主題下深度發展 → 指向同一區的不同子節點
- **每張入口卡應導向不同方向，才有意義**——不要四張都指向同一個分支

```
以 Index 20 - 卡片筆記 為範例：

連結
[[20. 落地生根：每一份筆記都不斷往下相連，形成思維鏈]]


卡片筆記法
[[20. 卡片筆記盒要帶來的美好滿足：完整原則]]


卡片
[[20. 卡片筆記的標題，是把大概念壓縮成一句話]]


LLM 與卡片筆記
[[20. 將 AI 結合落地到卡片筆記盒的初步構想]]
[[20. 語義搜尋：超越文本的搜索與連結功能]]


```


**AI 操作指引**：
- 不要把索引頁做成目錄式的臃腫清單
- 索引頁整理的目標：把大量卡片收斂成少數關鍵字，每個關鍵字背後 1~4 張精選入口卡，用連結遊走解決查找的問題。
- 如果某個關鍵字下連結超過 4 張卡片 → 拆分出子關鍵字，或篩選出最有代表性的入口

---

### 操作卡片筆記時的核心直覺

當你要幫加恩做操作判斷時，回到這五條去想——不要讓「看起來整齊」「看起來完整」的直覺蓋掉原則。魯曼的系統之所以有效，正是因為它反直覺地**不整齊、不完整、速度優先**。

```
【   TYPICAL CARD-LINK STRUCTURE   】 

          ┌──────────────────────────────┐
          │  INDEX (keywords, sparse)    │
          └──────────────────────────────┘
             [alpha]            [beta]
                │                  │
                ▼                  ▼
            ┌───────┐          ┌───────┐
            │   A   │          │   B   │
            └───┬───┘          └───┬───┘
                │                  │
           ┌────┴────┐        ┌────┴────┐
           ▼         ▼        ▼         ▼
         ┌────┐  ┌────┐    ┌────┐   ┌────┐
         │ a1 │  │ a2 │    │ b1 │   │ b2 │
         └──┬─┘  └──┬─┘    └──┬─┘   └──┬─┘
            │       │         │        │
            ▼       ▼         ▼        ▼
         ┌────┐  ┌─────┐   ╔═════╗  ┌────┐
         │a1a │  │ a2a ├──►║ b1a ║  │b2a │
         └────┘  └──┬──┘   ║  *  ║  └────┘
                    │      ╚═════╝
                    ▼          ^
                 ┌─────┐       │
                 │ a2b │       │
                 └──┬──┘   (hub: many
                    │      paths converge)
                    ▼
                 ┌──────┐
                 │ a2b1 │
                 └──────┘

圖例
─────
 ──►     連結方向（閱讀遊走路徑；也是新卡沿用舊卡編號的方向）
 入口卡   由索引關鍵字指向的卡——可以是任何一張卡
          （深層子節點、人名卡、hub 卡都可以）
 ★／╔╗   hub：多入度的高連結度節點——隨多重儲存「自然長出」，不是設計的
          入口卡不必然是 hub；hub 也不必然剛好被索引指到
 多重     寫新卡時若想到多種連結可能，就全建——例如 a2a 寫時，
          同時把它接在 a2 後面、也建連結跨到 b1a（跨脈絡連結）
 反駁     發現錯誤不刪，新卡直接接在錯卡後面、沿用編號
          （例：a2b1 是 a2b 的子編號；不是另開獨立卡）


這張圖體現的五條原則
──────────────────
 一、節點粒度小      每張卡兩三句 → 整張圖由大量小節點組成
 二、局部連結不分類  a2→a2a→a2b→a2b1 是沿鏈走，編號連續沿用主題沿鏈自然分叉（下游可能已經跟 A 差很遠）
 三、多重儲存        a2a 寫時同時建了跨到 b1a 的連結
                     → 鏈與鏈被織成網，不是一串串獨立的鏈
                     → 這類跨連結累積，讓某些節點自然長成 hub
 四、不刪錯的卡      a2b 被判定錯也不刪，a2b1 接在它後面反駁
                     編號連續 → 反駁跟原卡物理相鄰、並存
 五、索引刻意不完整  α、β 只點亮 A、B 兩張入口卡
                     其餘節點由順連結遊走發現（不由索引窮舉）
                     入口卡就是「能快速帶人進入網絡」的卡——不一定
                     是 hub，有時是 hub，有時是深層子節點或人名卡

```



簡記：
1. 寫入：好讀易懂，簡短明白。
2. 連結：只問能連到什麼，別問屬於哪類
3. 儲存：想到幾種就建幾種
4. 錯誤：不刪、不合併，用反駁卡對話
5. 索引：關鍵字 → 1~4 張入口 → 順連結遊走；索引刻意不完整



## 關鍵規則

建立新檔案時，除非有明確指示，否則**一律預設存到 `001 INBOX/`**。

⛔ 中文文本一律使用全形逗號  
	✓  正確：「，」（全形逗號）
	✗  錯誤：「,」 （半形逗號）   
	中文檔名若出現半形逗號 = 檔案錯誤
		→ 破壞 wikilink 
		→ 破壞檔案引用 
		→ 破壞系統操作   
	**📋 執行範圍：** ✓ 所有中文文本，無一例外

⏳ **時間軸文件一律最新在最上、最舊在最下**  
	✓  新 entry 寫進 `## 📝 工作日誌`、`000 Skill 更新回饋.md` 的更動表、callout 內帶日期的條列、任何 `------------ YYYY-MM-DD ------------` 分隔的區塊時——**插在現有條目最上方**，往下越久越舊  
	✗  錯誤：append 到檔案最下方（時間軸會混亂、最新狀態被埋在最底下要捲老半天才看到）  
	**📋 執行範圍：** ✓ 所有帶日期／時間順序的寫入（工作日誌、更新回饋、領悟 callout、更動表、會議記錄⋯⋯），無一例外  
	**設計理由：** 最新狀態最常被讀到，要放在第一眼能看見的位置；歷史紀錄重要但屬於背景，往下捲查得到就好

## Obsidian 慣例

### 檔名前綴語意（加恩的命名默契）

從檔名前綴可以判斷筆記類型，這是貫穿整個 vault 的識別系統：

| 前綴 | 意義 | 主要位置 |
|---|---|---|
| **《xxx》** | 文獻筆記（書、文章、影片摘要） | `008 SOURCE/`、處理中可能在 `001 INBOX/` |
| **＃xxx** | 草稿（未完成的想法、長文草稿） | `009 草稿區/`、`001 INBOX/` |
| **數字 + 點**（`4. ` `15. ` `29. `⋯⋯） | 筆記卡片，編號對應 `Index N` | `005 CARDS/` |
| **【 名稱 】** | **WORKBOOK 主檔**——出現在 DASHBOARD 上的最高層級專案頁，有 frontmatter／現況／下一步／工作日誌完整結構 | `006 PROJECT CARDS/` |
| 無 【 】的專案檔 | WORKBOOK 底下的產出文件（卡片、草稿、SKILL 修改檔、會議記錄等） | `006 PROJECT CARDS/` |

⚠️ **【 】是區分 WORKBOOK 主檔 vs 產出文件的關鍵記號**——SKILL 判斷「該寫進哪個 WORKBOOK」時，只認 `【 xxx 】` 格式的主檔，避免抓到一堆有的沒的產出文件。

📌 這個 vault 的專案領域字母編碼（對應 `006 PROJECT CARDS/` 的檔名前綴）尚未定義，開始跑專案時再回來補上。

### 其他慣例

- 在 Obsidian 中，重新命名檔案會自動更新整個資料庫中的所有 `[[wikilink]]`，所以改檔名是安全的。當卡片需要改名時——例如歸檔進筆記網絡時補上或更改編碼前綴——AI 可以直接動手改檔名（用 `mv` 或重新命名），不必只改連結的顯示文字（那反而會讓連結壞掉）。改完記得一律用新檔名建立連結。
	- 可以直接改的編碼前綴包含：**數字編碼**（Index 編號，例如 `4. ` `15. `）與**英文字母編碼**（專案領域前綴）。補上、更改、或調整這些編碼都可以直接做。
- 筆記使用 Obsidian 的 `[[wikilink]]` 語法來建立內部連結
- **wikilink 只寫卡片名稱，不寫完整路徑**：建立連結時用 `[[卡片名稱]]`，不要寫 `[[001 INBOX/卡片名稱]]`。Obsidian 只要名稱唯一就會自動解析到正確檔案，寫路徑只會讓筆記看起來雜亂。（例外：同名檔案需要消歧義時才補路徑。）
- 卡片命名遵循索引對應：如果一張筆記屬於 `Index 4 - xxx`，命名為 `4. (筆記標題)`
- **卡片模板**：建立新卡片時，一律遵循 `007 EXTRAS/999 Template/0 Card Note.md` 的格式。卡片必須包含 `CREATED::` 時間戳、dataview 反向連結查詢，以及 `Source` 段落。
- **新卡片不加編號**：卡片剛建立在 `001 INBOX/` 時，檔名**不可以有 Index 編號前綴**（例如只用 `追求本身創造律法.md`，而非 `3.1 追求本身創造律法.md`）。編號代表卡片已經進入永久筆記盒（`005 CARDS/`）。
- 筆記主要以繁體中文撰寫；建立內容時請維持這個慣例


## Skill 維護規則

當你的工作任務涉及 SKILL 的編輯與創建，請參照 [[Skill 規則]] 。


## 使用者檔案

另存於 `CLAUDE-user-profile.md`。**請勿在每次對話時都讀取。**

以下情境再讀取這份檔案：
- 加恩明確要求（例如「讀一下你對我的理解」、「更新 profile」）
- 對話涉及人生方向、內在探索、寫作風格等深層話題，需要脈絡
- 需要判斷互動方式（例如該給建議還是該陪伴映照）

以下情境請勿讀取：
- 一般性的筆記整理、檔案操作、技術問題
- 短問短答、指令型任務

注意，另一個理解使用者的好方法是：調用最近期的 git log 紀錄，看看使用者最近在做哪些思考，工作了哪些筆記，做了什麼事情，你會獲得時間軸上的使用者理解。視情況決定你需要調動多少天的紀錄與文件。

